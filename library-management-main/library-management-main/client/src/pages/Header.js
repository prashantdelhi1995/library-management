import React from "react";

const Header = () => {
  return (
    <nav
      style={{
        backgroundColor: "darkslateblue",
        top: "0",
        fontSize: "25px",
        padding: "15px",
        fontWeight: "bold",
        color: "white",
        textAlign: "center",
      }}
    >
      Library Management System
    </nav>
  );
};

export default Header;
